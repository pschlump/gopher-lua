# Lua→Wasm Divergence Ledger

Companion to `docs/Lua-Wasm-Design-and-Test-Plan.md` §8.9.

**Rule: a divergence without a ledger row is a bug; a row without a test is a bug.**

Where the engines disagree (or any engine disagrees with C Lua 5.1, the
semantic authority), the ruling lives here. Rows record: what diverges,
why, the ruling, and the covering test or shim.

| # | What | Where | Why / ruling | Covered by |
|---|---|---|---|---|
| 1 | `math.randomseed` is a no-op on Go ≥ 1.24 | `mathlib.go:201-204` (`rand.Seed`) | Go 1.24 made `rand.Seed` a no-op by default (`GODEBUG randseednop`), so seeding does not reproducibly reseed the global source. Additionally the unseeded `math.random` stream is process-random, unlike C Lua 5.1 which starts deterministically (`srand(1)`-equivalent). **Ruling:** fork bug; fix is a per-`LState` `rand.New(rand.NewSource(...))` in mathlib. Until fixed, the testdiff shim replaces `math.random`/`math.randomseed` with an engine-owned deterministic source (required of every engine — the C oracle included). | `testdiff/interp.go` shim; caught by `sort.lua` in `TestSelfDiffLua51Tests` |
| 2 | `os.tmpname()` returns a process-unique path | `oslib.go` | Temp paths embed `/var/folders/.../T/<pid-ish>` and differ run to run; `verybig.lua`'s error message embeds the path. **Ruling:** harness shims `os.tmpname` to a fixed relative name, removed after the run; every engine must provide the same shim. | `testdiff/interp.go` shim; caught by `verybig.lua` |
| 3 | (design note) print-diffing is blind to string/number distinction | harness | `print(1)` and `print("1")` produce identical log lines (Lua print semantics: strings raw). Corpus asserts and GLOBALS serialization (strings quoted) cover the distinction in practice. **Ruling:** accepted blindness, documented. | `testdiff/normalize.go` `PrintArg` comment |
| 4 | `os.time`/`os.clock`/`os.date`/`os.execute`/`os.getenv`/`os.setenv` are environment-dependent | `oslib.go` | Wall clock, exit codes, and the process environment vary across runs and machines. **Ruling:** harness shims all six to constants/pure-map equivalents with Lua-conforming shapes; real semantics are tested by the C oracle's own suite, not by the diff. | `testdiff/interp.go` shim |
| 5 | **RESOLVED (M5d):** interp's os.time/os.date shims now implement the same pinned-instant semantics as luawasm.c — no-arg time = 946684800, table form does real UTC calendar arithmetic (days_from_civil, hour default 12, isdst=false written back), date formats the fixed 2000-01-01 instant (Saturday; strftime subset, time arg ignored). | `testdiff/interp.go` vs `runtime/luawasm.c` | self-diff + clua conformance green |
| 6 | Lua 5.1.5 `tonumber(s, base)` wraps negatives on ILP32 | `runtime/lua51/src/lbaselib.c` | `strtoul`/`unsigned long` is 32-bit on wasm32: `tonumber('-10', 36)` → 4294967260. Patched to `strtoll` (LP64 behavior). | `TestCLuaConformance` math.lua |

## M4 rows (2026-09-14 — backend v1)

| # | Divergence | Where | Notes |
|---|---|---|---|
| 7 | GLOBALS content differs between the interp oracle and the C/wasm engines by construction (gopher-lua adds `_GOPHER_LUA_VERSION`, `channel`, `table.foreach`, different `package`/`io` surface; `math.huge` prints `inf` vs `1.7976931348623e+308`) | engine library sets | Excluded from cross-engine diffs (`DiffLogs` drops GLOBALS lines). The globals surface aligns with the host module (M7). |
| 8 | Interp's uncaught-error payloads carry Go-side `stack traceback:` tails the C engines cannot produce | `interp.go` PCall error rendering | Stripped in `DiffLogs`; M5's byte-exact error gate compares message heads (chunk:line + message body). |
| 9 | **RULED (M5d): the runtime implements the fork's texts under the dialect flag.** `rt_set_dialect(1)` (wasm engine only; the clua oracle keeps stock C 5.1 texts, so the M2 gate is untouched). Families: arith `cannot perform <op> operation between <t1> and <t2>`, `__unm undefined`/`__len undefined`, concat `cannot perform concat operation between <t1> and <t2>`, index `attempt to index a non-table object(<t>) with key '<k>'` (key rides from luaV_get/settable), compare `attempt to compare <t1> with <t2>`, call `attempt to call a non-function object`, assert's strict string check, error()'s strict-string + no-arg check. Positions: rt line immediates + the activation line-stack (per-live-rt_run) with gopher's frame arithmetic (C frames transparent; the main chunk owns the bottom entry — it has no CallInfo). err00–err02 and tco06 now PASS. | ldebug.c/lvm.c/lauxlib.c/lbaselib.c hooks + rt_abi.c | `_wasm-err-tests` (99 cases, 91 comparable byte-exact) |
| 10 | `table.sort` on the wasm backend trips the wasm callback machinery (engine: wasmtime `index out of range` panic at the post-run globals dump; CLI: no output after the sort call, clean exit) | ltablib sort under rt_call | Root cause pending the M5 error/EH investigation (suspect: the sort path unwinding through EH-unaware script frames). `_wasm-tests/tbl07` skips on this row. |
| 11 | Register cells are not GC roots: any `luaC_checkGC` inside library calls (e.g. table.sort stack churn) can collect tables referenced only from cells | M3 ABI obligation, v1 backend | **CLOSED by posture (M6d, m6 plan §4.2):** fresh-VM-per-run + GC stopped per run + a hard per-VM allocation cap (row 36) + instance teardown freeing everything — no cross-run leak by construction and the GC-live use-after-free hazard (row 27's triage note) stays inert. GC-enabled/rooted mode is ruled out of v1; VM-pool reuse waits for M8's `rt_reset` measurement. |
| 12 | **CLOSED (M5a–M5c):** OP_CLOSURE/upvalues (A4), OP_VARARG (M5b), and the tailcall trampoline (M5c) all lowered — every opcode in the fork's set emits. | `luawasm/` | clo*/var*/tco* batches + m5{closure,vararg,tail}_test.go |

## M5a rows (2026-09-14 — closures + callback machinery)

| # | Divergence | Where | Notes |
|---|---|---|---|
| 10u | (M5e: unchanged then) `table.sort` (both with and without a wasm comparator) still tripped the callback machinery post-A4: engine panics with a Go `index out of range` propagated through wasmtime's enterWasm during the nested dispatch; comparator-less sort dies silently. gsub replacement functions, pcall/xpcall of wasm closures, and `__index` function metamethods passed through the adapter. | ltablib sort under rt_call + the precall adapter | **Fixed (2026-09-16, M6):** this was row 31's bug — sort grows the Lua stack across its nested `luaD_call`s (auxsort's lt/set2 churn), so call_body's dangling `base` corrupted every sort of nontrivial size; with the savestack/restorestack rebasing, plain sorts, comparator sorts (~1500 callbacks over 300 elements), `__lt` metamethod ordering, and error-raising comparators unwinding through pcall all pass byte-exact. `_wasm-tests/tbl07`, `cb00` unskipped + `tsort01.lua` added (matrix 516/516, 0 skips); `_cli-tests/tsort.lua` gates it end-to-end. |
| 13 | Latent M4 emitReturn bug: fixed-count RETURN staged results at ABSOLUTE addresses 0..16n (a bare `i32.const` for the destination), corrupting runtime static data; main-chunk returns rarely had B≥2 so the M4 corpus never saw it. Any called function returning values exposed it immediately (callee's untouched arg0 cell came back as the "result"). | `luawasm/ops.go` frameCellAddrConst | Fixed (frame-relative). Covered by every clo* multi-result case. |
| 14 | ABI reentrancy law (M5a callback machinery): an rt_* protected body that `luaD_call`s (rt_call fallback, metamethods in arith/len/cmp/concat/gettable) can re-enter the ABI — a wasm closure called through the adapter runs another rt_* before the outer body resumes, overwriting the shared argument statics. Symptom: outer rt_call wrote results into the inner's arg cells (callee echoed as result). | `runtime/rt_abi.c` *_body statics | Fixed: every body keeps its pointers/outputs on the C stack; only the single-word output is written back after the call. Covered by upcall/recursion cases (`TestM5Closures`). |
| 15 | Init ordering: upvalue descriptor rows (rt_wasm_upval) emitted before the child's rt_wasm_proto registration were silently dropped (registry bounds guard), leaving zeroed descriptors → `instack=0` captures with no parent → error. | `luawasm/backend.go` emitInit | Fixed: all protos register first, then the descriptor scan. Covered by every capture case. |
| 12u | OP_CLOSURE/GETUPVAL/SETUPVAL/CLOSE lowered (A4); OP_VARARG remains rejected (M5b). | `luawasm/emit.go` | SKIP-UNSUPPORTED retained for varargs only. |
| 16 | Scratch-cell collision on vararg protos: the callee-save/compare scratch at static R(nregs+2) can be overwritten by a multret VARARG's destination window (R(A..A+nv-1) extends past nregs — `g(2, ...)` saved its callee over the third vararg, which then traveled on as an argument). | `luawasm/emit.go` scratchAddr | Fixed (M5b): scratch = the adapter's +1 spare cell above the vararg staging area, frame+16*(nregs+4+nv), computed dynamically (Select, no live-local clobber). gFrameCells export now nregs+5 so lua_main's engine-allocated frame includes the spare. Covered by var13 (`constvarargs`). |
| 17 | When `...` is used, the compiler still reserves the `arg` local but never fills it (NeedsArg cleared at compile.go:1188, the local registered earlier) — `arg` inside such a function is the nil LOCAL, not the global. | fork compiler semantics | Matched by the backend (prologue skips rt_compat_arg exactly when NeedsArg is clear; R(np) nil-fills). Covered by var20 (`arg == nil` → true). The interp arg-global alignment ({[0]=chunkname}, matching luawasm.c's driver) covers true global references. |
| 18 | Deep `__call` chains with tailcalls corrupt the return path around the 3rd unwinding level (a dispatch returns a garbage nret, the next traps OOB; empty-message nil error surfaces). Plain `__call` and shallow chains (≤8) pass; plain deep staged tailcalls (10⁶) pass. Suspect: repeated tryfuncTM stack-hole insertion interacting with the adapter's frame/cursor accounting in the fallback path. | precall tryfuncTM + the rt_call tailcall fallback | **Fixed (2026-09-16, M6a verification):** this was row 31's dangling `base` in call_body (the same `luaD_checkstack` growth moving the Lua stack under a held pointer) — with the savestack/restorestack rebasing, `__call`+tailcall chains pass clean to depth 120. Pinned by `_wasm-tests/tco09.lua` (tco07's depth 5 stays as the shallow case). |
| 19 | Sticky rt-staging after a Lua-level catch: err_pending stayed set after pcall caught, so the next rt_* in the surviving wasm frame refused instantly with the stale error (found by M5c's error-through-tailcall test; every pcall-of-erroring-closure escaped — the corpus only had pcall-of-success). The A2 spike missed it: its driver chunk was C-interpreted, so nothing after the catch crossed the ABI. | rt_abi.c staging + ldo.c | Fixed: luaD_pcall's recovery calls rt_pcall_caught() (consume the staged error). Covered by m5tail errchain + tco06's shape. |

## M5d rows (2026-09-14 — error/EH fidelity)

| # | Divergence | Where | Notes |
|---|---|---|---|
| 20 | Stack-overflow depth/unwind: the interpreter harness allows 1024 frames; the wasm guard fires at RTW_MAX_DEPTH=150 adapter levels, inside the adapter with no rt line — and the throw's unwind through ~150 nested adapter levels did not reach pcall cleanly (so00 surfaced a nil-valued uncaught error; so01 trapped). | precall_wasm depth guard + call_body stack rebasing | **Fixed in M6 (2026-09-16):** the unclean unwind was row 31's dangling `base` — with call_body rebasing, so00/so01 propagate a clean pcall-caught "stack overflow" and pass byte-exact (skips removed; `_wasm-err-tests` 94/94, 0 skips). The depth VALUE divergence (150 vs 1024) remains a documented non-test divergence by design (LUAI_MAXCCALLS vs harness limits, plan §7). |
| 21 | Non-string uncaught error rendering: the wasm engine renders the staged TValue (nil/boolean/number deterministic — error(nil)/error(42) match interp byte-exact); table/function values carry engine-local addresses in interp's ValueRepr and stay outside the suite (in-script pcall + type covers them). | wasmengine.go renderErrValue | e006–e009 cover the deterministic tags. |

## M5e rows (2026-09-15 — full-suite gates)

| # | Divergence | Where | Notes |
|---|---|---|---|
| 22 | Coroutines: `coroutine.wrap/resume/yield` and yields across metamethod/C-call boundaries — out of scope for the Redis subset by design (plan §7.7; the general-purpose build needs the Asyncify/CPS study). | design scope | `_glua-tests` coroutine.lua, issues.lua skip on this row. |
| 23 | `debug.getinfo`/`getupvalue`-family introspection over wasm frames: registered Protos carry no C code or lineinfo, so currentline/linedefined/name resolution returns nothing. Core-raise error paths ARE safe (currentpc returns -1 and getfuncname returns NULL for wasm frames — the M5e hardening; previously `p->code[pc]` dereferenced NULL and corrupted the error machinery, nil-ing pcall-caught messages). | ldebug.c wasm-frame guards | db.lua skips on this row. Real debug parity needs the line-stack plumbed into getinfo (M7 tracebacks). |
| 24 | loadstring-mixed chunks: code compiled at runtime runs on the C lvm (not the backend), so its errors carry C positions/wording inside a wasm run; plus gopher parser-error texts differ (`register overflow` vs C 5.1's limit message). Ruling per the plan: unsupported until M6 (ruling unchanged). | wasm engine × ldostring/loadstring | goto.lua, vm.lua skip on this row. |
| 25 | Library-error wording/behavior: `math.max()` arity text (`wrong number of arguments` vs C arg error), `string.dump` (the fork rejects it; C implements it), `string.format("%d", v)` for v ≥ 2^31 (gopher formats the full double; C 5.1 casts through a 32-bit `int`, clamping — found by `_cli-tests` fib(48+)), `tonumber("1e2")` (gopher returns nil; C parses exponents — found by `_cli-tests` calc). The M5d dialect covers core raises only; library surfaces remain stock C. | lmathlib/lstrlib vs the fork's libs | math.lua, strings.lua skip on this row. Extending the dialect to lib errors is an M7 host-module concern (which replaces these libraries anyway). `_cli-tests` README rule: keep %d values < 2^31, no tonumber on exponent strings. |
| 26 | `#obj` with a `__len` metamethod: rt_len took the native path for tables without consulting TM_LEN (M3 gap; the matrix's len cases had no metatables). | rt_abi.c len_body | Fixed (M5e): the metamethod wins for any type. Covered by vm.lua's `__len` case. |

## M6 rows (2026-09-16 — CLI integration tests)

| # | Divergence | Where | Notes |
|---|---|---|---|
| 27 | Cumulative OP_CONCAT executions in one frame corrupt the run at ≥16: silent early exit with lua_main status 0 (output after the last completed concat is lost), staged garbage error bytes, or the row-10u nested-dispatch panic (`index out of range [5] with length 5` re-raised through enterWasm). Non-monotonic — with one module driven by an arg-supplied bound, n≤~32 died while n=80 died and n=90+ passed — layout-dependent, not a counter. No metamethods or callbacks involved: `for i=1,16 do t[i]="a".."b" end` suffices. First found by the `_cli-tests` grep clone over a 40+-line file. | runtime/rt_abi.c concat_body | **Fixed (2026-09-16, M6 triage):** `concat_body` popped the staging window with `top -= cc_n - 1`, leaving the residual result TValue on the Lua stack — a one-slot leak per rt_concat call (verified natively: `top - base` grew +1 per call). The crept top walked the stack past the frame until nested-dispatch machinery read garbage; the engine-side Go panic recover then discarded the whole event log (the "silent" mode). Every sibling body pops fully (gettable −2, settable −3); concat now does too (`top -= cc_n`). Native triage (`runtime/tests/rt_concattri_test.c`, ASan+UBSan) also caught a **GC-live use-after-free**: `rt_call` stepping the GC frees strings held only in ABI cells (cells are not roots — the documented row-11 contract; inert today because the engine stops the GC, a hazard for any GC-enabled mode; the test roots its operands). `_wasm-tests/concat16.lua` passes (skip removed); `_cli-tests` concatstress/grep2 promoted to check3. |
| 28 | pcall-caught core raises lose the position prefix on the value the script receives: interp returns `"chunk.lua:5: attempt to index..."` (stock C Lua prefixes the same way); the wasm path returned the bare `"attempt to index..."`. Script-raised `error("x")` kept its prefix (the error() line-stack machinery); only core raises raised inside rt_* bodies missed it — rt_run_raw prefixed `err_buf` for the uncaught/staged surface, but the TValue that pcall hands back to the script was staged before the prefix existed. Found by `_cli-tests/err1.lua`. | rt_abi.c stage_error / rt_run_raw prefix split | **Fixed (2026-09-16, M6):** rt_run_raw rebuilds the staged string value from the prefixed bytes and marks `err_prefixed` sticky so outer re-stagings don't accumulate prefixes (the first attempt double-prefixed through nested unwinds — the sticky flag was the missing half). `_wasm-err-tests/pc10.lua` passes (skip removed); so00/so01 also pass (their remaining diff was this row). |
| 29 | io.write's buffered tail was lost in both wasm-guest engines: the drivers return without libc `exit()`, so wasi-libc's atexit stdout flush never runs and only the first buffered line reached the host capture file (a >4KB write masked it by forcing flushes). Interp writes to the real process stdout, so any io.write-heavy script diverged. Found by `_cli-tests` caesar/bf/mandel/life. | wasmengine.go + clua.go stdout capture | Fixed (2026-09-16): both engines run a best-effort `io.flush()` dostring in the live state before reading the capture file (chunkname `=flush`, want_globals=0; failures ignored — e.g. a script that closed io.stdout). Covered by every io.write-based `_cli-tests` program. |
| 30 | Multiple assignment to locals evaluated sequentially, not simultaneously: `compileAssignStmtRight` passed the target local's register as the RHS compile destination, so `a, b = b, a+b` clobbered `a` before the second RHS read it (fib degenerated to powers of 2; `x, y = y, x` lost the swap). Inherited from upstream yuin/gopher-lua (v1.1.1 reproduces) and shared by interp and wasm — both wrong the same way, so every interp-vs-wasm gate stayed green; only the C oracle disagreed. Globals, upvalues, table targets and local declarations were unaffected; duplicate targets (`a, a = 1, 2`) also diverged (sequential gives 2, stock's reverse window gives 1). Found by `_cli-tests` fib vs clua. | compile.go compileAssignStmtRight | Fixed (2026-09-16): a local target whose old value a later RHS still reads (or that is assigned twice) evaluates into a temp window slot instead of direct-writing the live register; everything else keeps the original codegen. `_wasm-tests/masgn01.lua` pins the interp≡wasm lowering; `_cli-tests` fib runs against C Lua every `make test`. |
| 31 | calc-shaped programs die on the wasm backend in layout-dependent modes: silent rc=0 with no output at all, a memory-fault trap at a garbage address (runtime-built heap bytes read as a pointer), or an infinite hang before the first print. Root cause: **rt_abi.c call_body held a raw `StkId base` across `luaD_call`** — a nested precall's `luaD_checkstack` could grow and MOVE the Lua stack, leaving `base` dangling; `curL->top = base` then pointed the live state into freed memory, so every later stack write corrupted (whether realloc moved the block was heap-layout luck — 22-locals×depth2 died while 24×depth100 passes after pre-growing the stack, fib(15) died while fib(14) passed). Also explains the pre-M6 "non-monotonic recursion corruption near the limit" observation. Fixed with the same savestack/restorestack discipline precall_wasm uses; arith/len/cmp bodies were already safe (they re-read curL->top after their calls). Found by `_cli-tests` calc, minimized via sentinel probes + the pre-grow discriminator. | runtime/rt_abi.c call_body | **Fixed (2026-09-16, M6).** `_wasm-tests/calcshape.lua` passes (skip removed — matrix 513/513, 2 skips); `_cli-tests` calc1/calc2 promoted to check3; fat-frame recursion now clean to depth 149 (the RTW_MAX_DEPTH guard raises a proper error at 150+). |

## M6a rows (2026-09-16 — wazero production engine)

| # | Divergence | Where | Notes |
|---|---|---|---|
| 32 | **Ruling (M6a): one blob, two hosts — the EH-free flavor is dead.** The design's tracked M6 item ("EH-free blob flavor for wazero," plan A8/§5 — wazero was believed to implement no EH proposal, fallback = the M2 Asyncify path) is obsolete: wazero v1.12.0 — already pinned — implements the standardized exception-handling proposal behind `experimental.CoreFeaturesExceptionHandling`, in the same new-EH dialect the blob is built in (`-mllvm -wasm-enable-sjlj -mllvm -wasm-use-legacy-eh=false`). The existing `lua51_sjlj.wasm` validates, instantiates, and runs on wazero unchanged, because the M5 error-safety invariant keeps every throw inside the blob's own frames (compiled code returns `-1` + staged error; the adapter re-raises after the dispatch returns; no exception ever crosses a host or script boundary — asserted by the `wasm_dispatch` trap path returning `-3`). wasmtime stays the dev/CI differential oracle; wazero is the production engine. Perf note: darwin/arm64 wazero runs its interpreter engine (the conservative case); production targets linux/amd64+arm64 on the compiler engine — measured at M6b/M8. | testdiff/wazeroengine.go (production host), testdiff/wasmengine.go (oracle host) | `TestWazeroSpike` (14 EH-crossing cases incl. pcall/core-raise/sort-comparator/gsub/tailcall/`__call`, io.write as engine-parity vs wasmtime); `TestWazeroFullMatrix` (516/516), `TestWazeroErrorSuite` (94/94), `TestWazeroGluaFull` (3/3 + row 22–25 skips) |

## M6c rows (2026-09-16 — sandbox + determinism)

| # | Divergence | Where | Notes |
|---|---|---|---|
| 33 | **Sandbox surface (frozen list, m6 plan §4.3):** the production runtime flavor `lua51_prod.wasm` (`-DLUAWASM_PROD`, build.sh) opens base/table/string/math only — io, os, package/require/module, dofile/loadfile/load/loadstring, debug compile out (linit.c/lbaselib.c/lauxlib.c guards); `rt_sandbox(1)` (rt_abi.c, exported from BOTH blobs) nils the same ten globals at runtime for the dev blob, so a sandboxed dev engine is indistinguishable from prod. **Row 24 is closed by removal in production** (no runtime compilation surface exists to mix C-interpreted chunks); row 23 is moot in prod (`debug` absent). Dev engines keep rows 22–25 unchanged. The kept-allowlist `_G` key set is pinned by test (any change is a surface change and must update the pin). | runtime/{build.sh,rt_abi.c,lua51/src/{linit,lbaselib,lauxlib,ldo}.c,luawasm.c} | `TestM6cProdBlobArtifact` (import section = exactly the 5 host.*, rt_sandbox exported, SHA-256 pinned), `TestM6cSandboxSurface` (killed/kept lists + gopher-dialect index error, on prod-wazero, prod-wasmtime, dev-sandbox), `TestM6cSandboxGlobalsFrozen` (allowlist pin), `TestM6cSandboxMatrix` (dev+sandbox ≡ prod over `_wasm-tests` 517/517, 0 skips), CLI `interp,wazero-prod` legs 517/517 + 94/94 |
| 34 | **Zero-WASI host posture:** the prod blob has zero `wasi_snapshot_preview1` imports — the surviving link-time roots were musl's stdio cluster (lauxlib's loadfile undefineds; `print.c`'s printf; vfprintf/strtod's `__stderr_FILE` references whose FILE initializers embed ops function pointers — table-rooted data that function-GC cannot drop; eliminated by source exclusion, the `lua_atpanic` guard, `fopen`/`freopen` stubs, a strong `__stdio_exit_needed`, and `__stderr_FILE`/`__stdin_FILE`/`__stdout_FILE` byte-array preemption in luawasm.c, all under `LUAWASM_PROD`). Engines wire WASI/FS/stdout-capture only when the blob's own import section demands it (parsed per-run) — a leaked import fails instantiation loudly. Consequence: no io.* in prod means no STDOUT events by construction; `print` was already host-event-based, so PRINT parity is unaffected. The io.flush drain (row 29) is skipped under Sandbox (io is nil). | testdiff/{wazeroengine,wasmengine}.go, runtime/luawasm.c | `TestM6cProdBlobArtifact` (zero-wasi parse of the embedded bytes); the dev blob's 17 wasi imports are asserted as the parser sanity leg |
| 35 | **Determinism/seed contract:** every engine (interp, wasmtime, wazero) starts each run at host RNG seed 42 (the C `lnewstate` contract) and exposes a host-side `Seed` override applied after lnewstate — identically-seeded engines observe identical `math.random` streams (all three hosts draw from Go `math/rand`). The anchor values for seed 20260916 are pinned absolutely (the M6b moral: cross-engine agreement proves parity, an absolute value proves correctness); they change only if Go's stream does, loudly. | testdiff/{interp,wasmengine,wazeroengine}.go | `TestM6cSeedAnchor` (4 engines × pinned stream + different-seed sanity), `TestM6cDeterminism5x` (`_wasm-tests` ×5 byte-identical raw logs incl. GLOBALS), `TestM6cTwiceRunIsolation` (fresh VMs, GLOBALS serialization byte-identical), `TestM6cTail1e7` (10⁷ tail chain on both hosts: 0.5s wasmtime / 8.5s wazero) |

## M6d rows (2026-09-16 — memory cap + deadline)

| # | Divergence | Where | Notes |
|---|---|---|---|
| 36 | **Alloc-cap surface (D3):** the runtime's `lua_Alloc` is `rt_alloc` (rt_abi.c; `lnewstate` builds states with `lua_newstate(rt_alloc, NULL)`), budget-checked against the init-time `rt_set_memlimit(bytes)` — a host call made BEFORE lnewstate so the whole VM lifetime (state, stdlib, shims, constant pool, script) counts. A refused allocation flows through stock 5.1's `LUA_ERRMEM` machinery as the bare literal `not enough memory` (MEMERRMSG, no position — seterrorobj's shape) — an ordinary pcall-catchable Lua error, never a trap. Two engine-side wrinkles: (i) **ERRMEM staging** — stock `luaD_seterrorobj` only runs at pcall-family recoveries, so a plain `rawrunprotected` catch (every rt_* body) would stage the stale value at `L->top-1`; `stage_c_error` stages the literal instead; (ii) **emergency slack** — while `rt_oom_inflight`, allocations up to limit+64 KiB pass so the raise's own message-interning cannot dead-end the error machinery into error handling; the counter keeps counting, so catch-and-retry gains at most 64 KiB once; `rt_err_clear`/`rt_pcall_caught`/`rt_set_state` re-arm the refusal. The **interp has no counterpart**: Go's allocator cannot refuse (`SetMx` watches runtime stats and `os.Exit(3)`s — not catchable), so the text is pinned absolutely, not diffed. Second, coarser backstop: both blobs link with `-Wl,--max-memory=268435456` (256 MiB) — a `memory.grow` failure makes dlmalloc return NULL → the same clean path, and the declared max makes wasmtime map the memory statically (base never moves — the row-37 watchdog contract). Non-Lua allocations (frame chunks, constant buffers) bypass the cap by design (depth-guarded, restored per frame). | runtime/{rt_abi.c,rt_wasm.h,luawasm.c,build.sh}, testdiff/{wasmengine,wazeroengine}.go (`MaxMem`) | `TestM6dMemCap` (ERROR = bare `not enough memory`, bounded, no trap — wasmtime/wazero/prod), `TestM6dMemCapCatchable` (pcall catches, script continues, re-arm), `TestM6dMemCapSingleCall` (512 MiB `string.rep` under a 2 MiB cap: a single C call is poll-free but cap-bounded), native `runtime/tests/rt_native_test.c` M6d block (catchable ERRMEM, catch re-arms, unlimited again after `rt_set_memlimit(0)`) |
| 37 | **Deadline semantics (D4):** the host arms a control-block flag (`g_deadline_flag`, address via `rt_ctrl_addr()`) when the run's deadline expires — engines do it from a `time.AfterFunc` watchdog with a single aligned 4-byte store into the shared linear memory (wasmtime Stores are !Sync; the static-memory max from row 36 keeps the base fixed — the one safe off-thread crossing; wazero uses `Memory.Write`). The backend emits a poll (global.get the cached flag address → `i32.load` → `if set: rt_deadline` → return -1) at every proto's flattened dispatch-loop header — once per basic-block transition, the same granularity as the interp's per-instruction ctx check — and at the `lua_dispatch` tailcall-restage loop (tail spins never touch a basic block). `rt_deadline` stages `context deadline exceeded` — **body byte-identical to the interp oracle's cancellation text** (`mainLoopWithContext` raises `ctx.Err()`; `Interp.Deadline` exposes SetContext for the parity leg) — but **unprefixed**: the interp's raise rides an instruction boundary (chunk:line:), the wasm poll site has no line info. The flag is sticky for the run: pcall can catch the raise and a script that catches-and-finishes runs to completion on wasm, while the interp re-raises at every instruction post-deadline (its catch test dies at the next statement) — back-edge-gated vs instruction-gated re-raise is the ruled divergence. Measured kill latency for `while true do end` at an 80 ms deadline (darwin/arm64, whole Run incl. compile+instantiate): interp ~83 ms (ε≈3 ms), wasmtime ~107 ms, wazero ~131 ms, wazero-prod ~118 ms — the poll check interval is ≤ one basic block, so ε is dominated by engine setup and scheduler wake. Blind spot by design: a single non-returning C call never polls — bounded by the row-36 cap instead. Happy-path cost of the per-block poll: none measurable (fannkuch(9) wasmtime 1.01 s / wazero 24.8 s — identical to the M6b baseline; the wazero 10⁷-tailchain leg pays ~2 s for the restage poll). | luawasm/{backend.go,emit.go} (poll sites), runtime/rt_abi.c (flag + rt_deadline), testdiff/{wasmengine,wazeroengine,interp}.go (`Deadline`) | `TestM6dDeadlineKill` (4 engines: body-parity pin vs interp, ε<2 s hard bound, latency reported), `TestM6dDeadlineTailSpin` (restage-loop poll), `TestM6dDeadlineCatchable` (catch → complete; catch → loop → re-raise), `TestM6dDeadlineUnfired` (armed-but-unfired ≡ unarmed), native M6d deadline block (flag round-trip, staged text, sticky staging) |

## M6e rows (2026-09-17 — differential fuzzer bring-up)

All found by the M6e fuzzer (`luafuzz`) during bring-up probes and the first soak chunks. Rows 38–40 are ruled fork-vs-C formatting/wording families with generator scoping; **rows 41–44 are FIXED** (2026-09-17, same day — the soak clock gate "generator stops finding same-day bugs" satisfied at 10k cases).

| # | Divergence | Where | Notes |
|---|---|---|---|
| 38 | **MOSTLY FIXED (2026-09-19, row 49) — number→string coercion outside the print shim.** `tostring`, `..` and `table.concat` now render numbers exactly like the fork's `LNumber.String()` in the gopher dialect (runtime/gnumfmt.c hooked into `lua_number2str`; the clua oracle keeps stock `%.14g` at dialect 0). This closed every finite-value corner of the original row — integer-valued floats ≥ 1e15 (`1000000000000000` vs `1e+15`), Go-shortest fractions (`1.1666666666666667` vs `%.14g`'s 14 digits), `int64(-0.0)` sign drop (`.. (-0.0)` → `0`), and the non-finite TEXTS (`+Inf`/`-Inf`/`NaN`, reached via overflow like `1e308*10` since `math.huge` stays out of the surface). Direct `print(number)` was never affected (the shim canonicalizes via normalize.go NumRepr). **Still ruled, the one survivor: the `math.huge` VALUE divergence** — MaxFloat64 in the fork vs HUGE_VAL in C (`print(math.huge)` → `1.7976931348623e+308` vs `inf`, and comparisons/divergence beyond formatting) — fuzzer-scoped (gen.go never emits `math.huge`); the durable fix (a dialect-gated DBL_MAX push in lmathlib) stays parked with the M7 host-surface pass. Pinned: `_wasm-tests/gnf00.lua`, `gnf01.lua` + the native gnumfmt goldens (runtime/tests). | fork LNumber string conversion vs C `LUAI_NUMFFORMAT` — now `runtime/gnumfmt.c` | Was: Ruled fuzzer-scoped via the safeNumStrPool; the pool discipline proved leaky (computed arithmetic results reach tostring/concat through locals and table slots — the row-49 findings), which forced the durable fix forward from M7. The generator's safeNumStrPool restriction stays (defense in depth; it is now redundant for finite values). |
| 39 | **Hash-part iteration order**: `pairs`/`next` over string-keyed tables iterate sorted on interp, in C ltable slot order on wasm/clua (`for k,v in pairs({b=1,a=2,c=3})` → interp `b,a,c`, wasm `a,c,b`-order). Unspecified by Lua 5.1 (next order is implementation-defined); gopher-lua sorts, stock C does not. | ltable vs gopher table next | **Ruled: unspecified-semantics divergence — differential corpus (and the fuzzer) never iterates hash parts**; `pairs` allowed only over dense sequence constructors (identical order both sides). Row 7's GLOBALS note is the serialization-side sibling. Covered by luafuzz gen.go's dense-only pairs rule. |
| 40 | **Arith error text names the RHS's coerced type in the fork**: `"abc" + "10"` → interp `cannot perform add operation between string and number` (the RHS "10" was coerced to a number before the LHS failed) vs wasm (stock C behavior) `… between string and string`. Only the corner *non-coercible-string LHS × coercible-string RHS* diverges; bool/nil/table operands, both-non-coercible strings, and LHS-coercible cases are byte-exact (probed matrix a1.lua). | fork vm arith error path vs lvm.c raw-type naming (row 9 family gap) | **Ruled: open dialect gap — fuzzer-scoped** (binary-arith operands never scalar-local/t[k]/call reads, so a non-coercible string can never meet a coercible string in an arith slot; arith error paths keep coverage via riskyExpr's deterministic raises). Fix decision (make rt_* match the fork's coerced-type naming) deferred to the M7 host-surface pass with row 25. |
| 41 | **FIXED — constant and/or chains in condition position mislowered.** `while ("A" or "hello") do … end` never ran its body; a nested `if (("2" and true) or "")` silently ended the run. Root cause: the compiler emits TEST/EQ/LT/LE/TESTSET with a JMP partner, but the finish pass rewrites a jump-to-next partner to **OP_NOP leaving the operand bits stale** (compile.go:1909, SetOpCode only); the interpreter executes the NOP as a no-op and falls through to pc+2, while the backend decoded the stale bits as sBx and landed on blockOf's last-block fallback (skipped bodies / accidental early return). Fix: `branchPartner(pc)` in emit.go/ops.go — JMP partner → its target, NOP partner → pc+2 (mirroring the interpreter). | luawasm/{emit.go,ops.go} branchPartner | **Fixed (2026-09-17).** Pinned by `_wasm-tests/whlc00-03.lua` (passing, no skip); const and/or conditions restored to the fuzz surface. |
| 42 | **FIXED — and it was the INTERP that was wrong (two upstream fork bugs, no pool corruption).** (a) `allocator.LNumber2I`'s preload fast path checked `v >= 0` — IEEE-true for **-0.0** — so every COMPUTED -0.0 was silently canonicalized to preloads[0] (+0.0): `x*2` with x=-0.0 printed `0` on interp vs `-0` on wasm (which matches stock C). (b) `funcContext.ConstIndex` deduped constants with interface `==`, which folds `0` onto a `-0.0` slot (whichever literal interned first wins) — so `t(0)` in a chunk containing `-0.0` actually passed -0.0, making the bug look layout/chunkname-dependent. Fixes: `!math.Signbit` in the preload guard (alloc.go); sign-aware `constEqual` for the dedup (compile.go). | alloc.go LNumber2I, compile.go ConstIndex | **Fixed (2026-09-17).** Pinned by `_wasm-tests/nz00.lua`, `nz01.lua` (passing, no skip); -0.0 restored to the fuzz surface (arith/print positions; `..`/tostring stays row-38-scoped). |

| 43 | **FIXED — modulo semantics: stock floor-formula vs the fork's fmod+adjust.** Stock 5.1's `luai_nummod(a,b) = (a) - floor((a)/(b))*(b)` double-rounds: `3 % 0.1` → `0` on the C side (3/0.1 rounds to exactly 30.0) vs the fork's `luaModulo` (math.Mod + sign adjust) → `0.0999999999999998` → `0.1`; `-0.0 % x` → `0` vs `-0`; runtime-operand forms hit it while literal forms were compile-time folded by the Go frontend (which uses luaModulo) — 38 of the first 5k soak findings were this one family. The interpreter is the spec, so the runtime mirrors the fork: `luai_nummod` in the vendored luaconf.h is now fmod + sign-adjust (blob rebuilt; prod SHA pin updated to 1e36a6de…b0a6c). | runtime/lua51/src/luaconf.h luai_nummod | **Fixed (2026-09-17).** Covered by the fuzzer surface (`%` over runtime operands) and `runtime/tests` rebuild; `_wasm-tests` 523/523 + err 94/94 green on the rebuilt blob. |
| 44 | **FIXED — interp closed SURVIVING frames' upvalues at error raise.** `raiseError`/`Error` called `closeAllUpvalues()` when no error handler was set — closing the open upvalues of frames BELOW the pcall too. After a caught error, a surviving closure's SETUPVAL wrote the closed copy while the owning frame's register kept the stale value: pcall-then-`v0 = 100` lost the write on interp (`print(v0)` → the old value) while wasm matched stock C (luaD_pcall's luaF_close only closes above old_base). Fix: no raise-time close; PCall's recovery closes exactly the dying frames' upvalues (`closeUpvalues(base)` — every frame the pcall pushed lives at or above base; upvalues are index-based so nothing dangles in between; also closes the pre-existing xpcall-path hole where NO close ever ran). | _state.go raiseError/Error/PCall (regenerated state.go) | **Fixed (2026-09-17).** Covered by the fuzzer surface (pcall-wrapped writes + shared-upvalue reads) and the root-package test suite. |
| 45 | **FIXED — 1-byte TValue tag stores corrupted recycled frame cells.** The backend wrote cell tags with a single-byte store (`i32.store8 offset=8`), but `tt` is a 4-byte int that the C runtime compares whole (`ttype`). Fresh frame-chunk memory is zeroed, so this hid for 300k+ cases — until the top-level rt_call cycle (every adapter unwind to `saved=0` frees the 1 MiB frame chunk, and the next push re-mallocs it over recycled heap) hands back memory with stale string bytes: `false` staged over ASCII `"defi"` produced tt `0x69666501` → pcall printed `(true, nil)`; ≥15-deep recursion crossing a chunk free/re-malloc made a later `pairs` read `t[3]`'s bool as nil. Found as fuzz findings c0023746/c0102544/c0241401/c0287123 (layout-dependence was just "did the recycled chunk land on dirty memory"). Fix: all seven tag writes in luawasm now use full 4-byte stores (emit.go setBoolCellA/prologue-nil-fill/LOADNIL; ops.go arith-fast-path/UNM-fast-path/forloop-var/multret-nil). | luawasm/{emit.go,ops.go} | **Fixed (2026-09-18).** Pinned by `_wasm-tests/tag00.lua` (pcall chain), `tag01.lua` (depth-15 + pairs). |
| 46 | **FIXED — capture pseudo-instruction could become a basic-block leader.** The compiler leaves UNREACHABLE jumps (dead code under constant conditions — `while false` under `if (nil and "x")` under a constant-false `while`) with stale BACKWARD targets, and such a target can land on an OP_CLOSURE's upvalue-capture pseudo-instruction. `partitionBlocks` skips pseudos while scanning but `leaders[jumpTarget(pc)]` marked the pseudo-pc as a block start; the block then EMITTED the pseudo as real code — its GETUPVAL clobbered the closure register and `f1(false)` died with "attempt to call a non-function object" (findings c0023970/c0119720/c0179047). The interpreter is immune twice over (it executes closure pseudos as part of OP_CLOSURE, and the dead jumps never run). Fix: `markPseudos`/`realPc` normalize every leader target and `blockOf` lookup past pseudo regions. | luawasm/emit.go partitionBlocks/blockOf | **Fixed (2026-09-18).** Pinned by `_wasm-tests/psd00.lua`. |
| 47 | **FIXED — interp parsed leading-zero decimal strings as OCTAL.** `parseNumber` (the fork's string→number coercion for arith/concat-fold/LVAsNumber) used Go `ParseInt(s, 0)`, which reads `"0255"` as octal 173 — `-(0 .. 255)` printed `-173` on interp vs `-255` on wasm/stock (finding c0213102; `tonumber` was never affected — it has its own parser). The interpreter is the spec's authority but stock 5.1 (`luaO_str2d`/strtod) and the wasm engine agreed against the fork, so the FORK was fixed: parseNumber now ports luaO_str2d semantics (leading zeros stay decimal; sign-aware 0x hex ints keep working; underscores rejected — Go literal syntax is not Lua's). | utils.go parseNumber | **Fixed (2026-09-18).** Pinned by `_wasm-tests/oct00.lua`. |
| 48 | **Ruled — pow is a libm implementation divergence.** Go's `math.Pow` (interp; repeated squaring) vs musl's correctly-rounded `pow` (runtime) disagree by 1 ulp on roughly a third of operand pairs — even `0.1^2` (`0.010000000000000002` vs `0.01`; finding c0107453, `x^0.1` last-digit). musl is the mathematically right side, but the fork is the oracle by contract, and byte-parity would require porting Go's exp/log/pow (~400 lines) into the runtime. | Go math.Pow vs musl pow (luai_numpow) | **Ruled: documented libm divergence — fuzzer-scoped** (luafuzz gen.go emits no `^`; the pinned `_wasm-tests/ari*` pow cases all agree and stay). Durable fix (port Go's exp/log/pow for byte-parity) parked with M8. |
| 49 | **FIXED — the 2026-09-18 fresh-clock findings: 5 × number→string DIVERGE (row 38's pool discipline leaked) + 1 × generator HANG.** (a) Five DIVERGEs in one family — c0071344 (`tostring(1e15+100)` → `1000000000000100` vs `1.0000000000001e+15`), c0750767 (`tostring(3.5/3)`, 17 digits vs 14), c0916336/c1066545 (`tostring` of 2^53-valued results → `9007199254740992` vs `9.007199254741e+15`), c1034921 (`tostring(2/255)` → `0.00784313725490196` vs `0.007843137254902`). All five put a COMPUTED arithmetic result into tostring via a local/table slot — row 38's safeNumStrPool only constrains literals, and any expression position can carry an arbitrary double, so the scoping was unenforceable at that granularity. Durable fix (row 38's M7 item pulled forward): `runtime/gnumfmt.c` ports the interp oracle's exact algorithm — Go 1.27 `internal/strconv` unrounded-scaling shortest-'g' (research.swtch.com/fp; BSD) plus `LNumber.String()`'s integer fast path with arm64 saturating float64→int64 (tostring(2^63) = `9223372036854775807`). Hooked as `lua_number2str` (dialect-gated: clua keeps %.14g) and shared with rt_grepr for error-text key reprs. Validated 0 mismatches against the fork over 7.9M values (6M random bit patterns, binade/decimal boundaries, pool-derived). (b) HANG c0158534: `stmtRepeat` never incremented `loopDepth` (nor had the ≥3 nest cap), so loops under a `repeat` were sized one level shallower — `repeat(7) × for(43) × while(31) × for(5)` ≈ 100k print events ≈ 2.3 s on the wasm leg vs the 1.5 s deadline (throughput, not correctness: interp finished in ~0.3 s; with a 60 s deadline both engines agree). Fix: repeat counts (plus the cap), depth-0 downward `for` start ≤ 42, while-limit 30 only at top level. Measured effect: 5000-case print tail max 36,652 → 3,621 (deadline capacity ≈ 60k). | runtime/{gnumfmt.c,gnumfmt.h,gnumfmt_pow10.h,rt_abi.c,luaconf.h lua_number2str,build.sh}, luafuzz/gen.go | **Fixed (2026-09-19).** Pinned: `_wasm-tests/gnf00.lua` (the five finding values through tostring/concat/table.concat), `gnf01.lua` (formatter boundaries incl. ±2^63 saturation, ±Inf/NaN texts, the 1e±6 %v cutoffs), native gnumfmt goldens (plain/ASan/UBSan), and `luafuzz.TestGenerateEventBudget` (3000-case print tail ≤ 20k). |
